{
  "version": 2,
  "builds": [
    {
      "src": "Dockerfile",
      "use": "@vercel/docker"
    }
  ],
  "env": {
    "NODE_ENV": "production",
    "PORT": "3000"  // Asegúrate de que este puerto coincida con el puerto expuesto en tu Dockerfile
  },
  "routes": [
    { "src": "/(.*)", "dest": "/" }
  ]
}
